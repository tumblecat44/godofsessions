# No-run reasons belong in the answer

When an overnight plan contains zero actionable candidates, render the
planner's leading project exclusions directly in the empty answer card.

Do not make the operator cross quota and route diagnostics to discover why
doing nothing is the recommendation. Reuse the exact typed exclusion records
instead of generating a second UI-only explanation.
